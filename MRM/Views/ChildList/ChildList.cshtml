@model MRM.Database.Model.ChildCampaign
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "Child Campaining List";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
</head>
<body>
    <div>
        @using (Html.BeginForm("ChildList", "ChildList", FormMethod.Post))
        {
            <h4>Child Campaigning Details</h4>
            <hr />
            <table cellpadding="0" cellspacing="0" class="table table-bordered">
                <thead class="header-background-color">
                    <tr>
                        <th>ID</th>
                        <th>Name</th>
                        <th>Campaign Desc.</th>
                        <th>Status</th>
                        <th>Start Date</th>
                        <th>End Date</th>

                    </tr>
                    </thead>
                    @foreach (var item in Model.ChildCampaigns)
                    {
                <tbody>
                    <tr>
                        <td>@item.Id</td>
                        <td>@item.Name</td>
                        <td>@item.CampaignDescription</td>
                        <td>@item.Status</td>
                        <td>@item.StartDate</td>
                        <td>@item.EndDate</td>
                    </tr>
                </tbody>
                    }
</table>
            <br />
            <table cellpadding="0" cellspacing="0">
                <tr>
                    @for (int i = 1; i <= Model.PageCount; i++)
                    {
                        <td  class="td-padding">
                            @if (i != Model.CurrentPageIndex)
                            {
                                <a href="javascript:PagerClick(@i);">@i</a>
                            }
                            else
                            {
                                <span>@i</span>
                            }
                        </td>
                    }
                </tr>
            </table>
            <input type="hidden" id="hfCurrentPageIndex" name="currentPageIndex" />
        }
        <script type="text/javascript">
            function PagerClick(index) {
                document.getElementById("hfCurrentPageIndex").value = index;
                document.forms[0].submit();
            }
        </script>
    </div>
</body>
</html>